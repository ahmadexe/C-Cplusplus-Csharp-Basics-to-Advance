#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
   
    char temp[999];
    char s[] = "Ahmad";
    char c[99];

    int len = strlen(s);
  

    for (int i = strlen(s)-1; i >= 0; i--)
    {
        for (int j = 0; j = len; j++)
        {
            printf("%d",j);
        }
        
    }
    
    
    return 0;
}